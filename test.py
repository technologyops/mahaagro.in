#!/usr/bin/env python# Gmail SMTP script by joon# Snippets from the following codes were used:#  http://www.go4expert.com/forums/showthread.php?t=7567#  http://docs.python.org/library/email-examples.html?highlight=sendmail#  http://djkaos.wordpress.com/2009/04/08/python-gmail-smtp-send-email-script/import smtplibfrom email.mime.text import MIMETextimport cgiprint ("Content-type: text/html \n\n")form = cgi.FieldStorage()userName = form["username"].valueemail = form["email"].valuecompany = form["company"].valuecontact = form["contactno"].valueaddress = form["address"].valuemessage = form["message"].valueprint  "Hai %s, your Email Sent Successfully..." % userNamesender = emailrecipients = 'pandi@mahaagro.in'uname = "Name:" + userName + "\nEmail:" + email + "\nCompany:" + company + "\nContact No:" + contact + "\nAddress:" + address + "\nMessage:" + message msg = MIMEText(uname)msg['Subject'] =  'Email From mahaagro.in'msg['From'] = sendermsg['To'] = recipientssmtpserver = 'smtp.gmail.com'smtpuser = email         # set SMTP username heresmtppass = 'ihtnayaj'   # set SMTP password heresession = smtplib.SMTP("smtp.gmail.com", 587)session.ehlo()session.starttls()session.ehlo()session.login(smtpuser, smtppass)smtpresult = session.sendmail(sender, [recipients], msg.as_string())if smtpresult:  errstr = ""  for recip in smtpresult.keys():      errstr = """Could not delivery mail to: %sServer said: %s%s%s""" % (recip, smtpresult[recip][0], smtpresult[recip][1], errstr)  raise smtplib.SMTPException, errstrsession.close()